# CLAUDE.md はClaude Codeが自動認識するため追跡する
# 以下は不要なファイルの除外ルール

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# agent-sandbox: 個人カスタマイズ
tools/agent-sandbox/compose.override.yml

# agent-sandbox: supervisor 共有ディレクトリ (IPC / worktree / audit)
tools/agent-sandbox/shared/
